export const siteConfig = {
  palette: "snow",

  effects: {
    mist: true,
    grain: false,
    snow: false,
  },

  home: {
    showMark: true,
    showTagline: true,
    tagline: "notes to self.",
    showHint: true,
  },
} as const;
